
'use client';

import { CalendarPageContent } from '@/components/dashboard/tabs/calendar-tab';

// This is now a simple wrapper around the new tab content component
export default function CalendarPage() {
    return <CalendarPageContent />;
}
